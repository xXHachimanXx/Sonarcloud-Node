function sum(a, b) {
    return a + b;
}

function sub(a, b) {
     return a - b
}

function times(a, b) {
     return a * b
}

function sumX(a, b) {
     return a + b + a
}

module.exports = {
    sum,
    sub,
    times,
    sumX
};
